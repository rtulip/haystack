include "std.hay"
fn main() {

    "hello World!\n" var [word]
    0 while dup word::size < {
        var [i]
        word::data i ptr+ @ print
        88 cast(u8) word::data i ptr+ !
        i 1 + 
    } drop

    word write

}