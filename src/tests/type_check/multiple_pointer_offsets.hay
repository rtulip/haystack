include "std.hay"

struct Foo {
    pub u64: bar
    pub bool: baz
}

struct Bar {
    pub *Foo: foo_p
}

fn work(*Bar: bar) {
    bar::foo_p::bar @ putlnu
    bar::foo_p::baz @ putlnb 
}

fn main() {

    12345 true cast(Foo) as [foo]
    &foo cast(Bar) as [bar]
    &bar work

}