
include "std.hay"
include "opt.hay"
include "selfhost/src/error.hay"
include "selfhost/src/ast/stmt/stmt.hay"

fn compile_haystack(HString: input_path bool: run) -> [Opt<HayError>] {

    &input_path Stmt.from_file_with_prelude drop

    Opt.None::<HayError>

}