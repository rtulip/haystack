include "std.hay"
include "file.hay"

fn cat(Str: path) {
    path File.str_from_path as [file_str]
    file_str putlns 
    file_str split cast(Arr) free 
}

fn main() {
    "./src/tests/cat.hay" cat
}