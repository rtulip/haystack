include "std.hay"

struct Pair<T> {
    T: first 
    T: second
}

struct Quad<A B> {
    Pair<A>: a
    Pair<B>: b
}

fn main() {

    // stack: [u64 u64] => Pair<u64>
    1 2 cast(Pair)

    "World\n" "Hello\n" cast(Pair)
    cast(Quad)

    split
    split write write
    split print print 
}