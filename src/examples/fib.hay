fn drop<T>(T: t) {}

fn dup<T>(T: t) -> [T T] {
    t t 
}

fn swap<A B>(A: a B: b) -> [B A] {
    b a
}

fn fib(u64) -> [u64] {
    dup 2 > if {
        var [x]
        x 1 - fib
        x 2 - fib +
    }
}

fn main() {

    0 while dup 20 < {
        var [i]
        i fib print
        i 1 +
    } drop
}