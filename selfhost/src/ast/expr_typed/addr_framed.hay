include "hstring.hay"
include "opt.hay"
include "tuple.hay"
include "vec.hay"
include "selfhost/src/types/framed.hay"

struct TypedAddrFramedExpr {
    pub Vec<Tup2<HString FramedType>>: frame
    pub u64                          : idx
    pub Opt<Vec<HString>>            : inner
}