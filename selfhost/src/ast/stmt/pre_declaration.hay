include "opt.hay"
include "vec.hay"
include "selfhost/src/ast/arg/untyped.hay"
include "selfhost/src/lex/token.hay"
include "selfhost/src/types/record/record_kind.hay"

struct PreDeclarationStmt {
    pub Token: token
    pub Token: name
    pub RecordKind: kind
    pub Opt<Vec<UntypedArg>>: annotations
}
