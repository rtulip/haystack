
fn foo(u64: n) -> [u64 u64 u64] {
    n 
    n 2 * 
    n 3 *
}

fn bar(u64: n) -> [[u64 u64 u64]] {
    [n foo]
}

fn main() {
    [1 true] println
    ["Hello" "Tuples"] println

    3 foo cast([u64 u64 u64]) println
    5 bar println
}