include "std.hay"
include "file.hay"

fn cat(Str: path) {
    path File.str_from_path as [file_str]
    file_str putlns 
    // Note: this leaks some memory
}

fn main() {
    "./src/tests/functional/cat.hay" cat
}