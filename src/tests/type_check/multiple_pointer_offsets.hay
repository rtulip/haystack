struct Foo {
    pub u64: bar
    pub bool: baz
}

struct Bar {
    pub *Foo: foo_p
}

fn work(&Bar: bar) {
    bar::foo_p::bar @ println
    bar::foo_p::baz @ println 
}

fn main() {

    12345 true cast(Foo) as [mut foo]
    *foo cast(Bar) as [bar]
    &bar work

}