include "hstring.hay"
include "tuple.hay"
include "vec.hay"
include "selfhost/src/types/framed.hay"

struct TypedFramedPtrOffsetExpr {
    pub Vec<Tup2<HString FramedType>>: frame
    pub u64                          : idx
    pub Vec<HString>                 : inner
}