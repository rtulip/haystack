var [u64 u64][15]: ArrayOfTuples_p

fn main() {

    ArrayOfTuples_p @ as [tuples] 
    0 while dup 15 < do {
        as [i]
        i i 2 * cast([u64 u64]) i tuples Arr.set
        i 1 +
    } drop

    0 while dup 15 < do {
        as [i]
        i tuples Arr.get println
        i 1 + 
    } drop

}