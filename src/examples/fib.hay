fn drop<T>(T: t) {}

fn dup<T>(T: t) -> [T T] {
    t t 
}

fn swap<A B>(A: a B: b) -> [B A] {
    b a
}

fn fib(u64) -> [u64] {
    dup 2 > if {
        dup 1 - fib swap
        2 - fib +
    }
}

fn main() {

    0 while dup 10 < {
        dup fib print
        1 +
    } drop
}